<Application x:Class="NUnitBenchmarker.UI.App" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" StartupUri="Views\MainWindow.xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" d1P1:Ignorable="d"
             xmlns:d1P1="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:NUnitBenchmarker.UI.ViewModels"
             xmlns:valueConverters="clr-namespace:NUnitBenchmarker.UI.Views.ValueConverters">
    <Application.Resources>
        <viewModels:ViewModelLocator x:Key="Locator" d:IsDataSource="True" />
        <Style TargetType="ToolTip">
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding}"
                                       Width="240"
                                       TextWrapping='Wrap' />
                        </StackPanel>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <valueConverters:GridLengthValueConverter x:Key="GridLengthValueConverter"></valueConverters:GridLengthValueConverter>
    </Application.Resources>
</Application>